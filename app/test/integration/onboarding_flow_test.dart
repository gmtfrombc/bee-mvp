import 'package:flutter_test/flutter_test.dart';

void main() {
  group('Onboarding flow integration', () {
    testWidgets('shows onboarding for new users then home after completion', (
      tester,
    ) async {
      // TODO: Complete full integration once onboarding UI available.
    }, skip: true);
  });
}
